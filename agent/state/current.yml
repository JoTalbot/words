project: words
milestone: M0
status: server_gate_verified; exit_gate_automated; m1_server_slice_batches_1-5_done
completed_tasks:
  - Deterministic board generator (prng golden vectors)
  - Dictionary snapshots en/ru/uk v2, canonical-locked by compiler test
  - Deterministic scoring engine (golden)
  - Authoritative 1v1 simulation (waves/claims/locks/steals/replay)
  - Match rooms + 30 Hz loop; terminal over=true snapshot on match end
  - WebSocket transport + protobuf; byte stability; client_sequence echo
  - Reconnect/resume inside GraceTicks=300 (canonical snapshot)
  - Network fault matrix RTT 50/100/150 ms, loss 0/1/3 % green
  - Load baseline (docs/LOAD-BASELINE.md)
  - Exit-gate automation: TestExitGateRepeatedFullMatches (6 full live WS
    matches; identical terminal state; scores == offline replay)
  - headless-bot executable harness (server/cmd/headless-bot) — live
    EXIT-GATE: PASS on the dev server 2026-09-09 (6/6 matches)
  - Dictionary compiler server/cmd/dictcompile (+ canonicality test)
  - Replay fixture seed 1512; web-m0 client proof; M1-ARCH-PREP doc
  - GitHub CI green on integrated main
  - Batch 1 (M1-prep): GET /v1/matches/{id}/result (persistence, TTL 5 min),
    WORDARENA_MAX_ROOMS cap (429), WS read limit, request logging + tests
  - Batch 2 (M1-prep): GET /metrics counters, graceful shutdown, Dockerfile +
    docker-compose.yml, TestMetricsCounters, local binary smoke test
  - Batch 3 (M1): basic matchmaking (POST /v1/queue, GET /v1/queue/{id},
    FIFO pairing per language, 2-min TTL, one-shot delivery) + replay
    endpoint (GET /v1/matches/{id}/replay, deterministic event log)
  - Batch 4 (M1-prep): background reaper (matchmaker queue + result store
    TTL sweep) + per-seat intent rate limiting (WORDARENA_INTENTS_PER_SEC,
    default 60/s, policy-violation close) + tests
  - Batch 5 (M1): player profiles (POST /v1/players, GET /v1/players/{id})
    + stats folding from finished matches via optional player_ids on match
    creation (wins/losses/draws/total_score) + tests
  - Unity CI pipeline fixed: in-container unity-editor build + email/password
    activation (game-ci gap + container.image env-context validation error +
    project CWD); documented in docs/unity-ci.md
active_tasks: []
blocked_tasks:
  - B1: Unity/Android build — pipeline is CORRECT now, but Unity Licensing
    API returns {"message":"Invalid Credential","code":"143.002"} for the
    current UNITY_EMAIL/UNITY_PASSWORD GitHub secrets (verified fresh
    2026-09-09 runs 34390832207, 34392443246). Fix = valid Unity account:
    verified email, correct password, no MFA/SSO on the automation account.
    Once creds work, build + hosted-emulator smoke test are unblocked.
  - B2: uk dictionary GPL-3.0+ derivative — legal review before distribution
    (dictionary/NOTICE.md)
failed_tasks: []
next_tasks:
  - Re-run Unity Android dispatch once UNITY credentials are fixed
  - M1: combo + Sudden Death rules (opt-in per match; M0 default unchanged),
    shared board UX, claim/lock/steal UI (client)
  - M1: durable player-profile/result persistence (PostgreSQL), profile in
    matchmaking (queue with player_id), session-token rotation
  - Android QA fallback hierarchy once an APK exists (hosted emulator wired)
last_commit: "dfb41d3"
tests:
  server: green on x86_64 sandbox (go1.27.1: go vet + go test -count=1 ./...
    all packages; cmd/game ~43 s incl. exit-gate) and on OCI arm64 host
    (go1.27.1); race clean on cmd/game (profile/matchmaking/hardening/metrics)
  ci: green through dfb41d3 (GitHub Actions, go1.24)
  docker: image NOT built on the shared OCI host (build attempt destabilized
    it once); local static-binary smoke test passed. Build the image only on
    a quiet host or in CI.
android_backend: unavailable for QA until APK exists — local emulator absent
  on ARM64 host (no emulator pkg / system-images / /dev/kvm); GitHub-hosted
  x86_64 emulator (reactivecircus, API 35) is wired and will run the smoke
  test once the APK builds; remote providers (myandroid.org etc.) remain
  interactive-only and were not bypassed
workers:
  - OCI dev service rebuilt and healthy (127.0.0.1:18080, binary @ dfb41d3,
    healthz ok, profiles/metrics/matchmaking/result endpoints live)
  - ollama on OCI host (qwen2.5:0.5b etc.) — use only for micro-tasks
resume_from: "Recover: repo state on GitHub (main @ dfb41d3). OCI host is
  healthy again (/opt/words @ dfb41d3, dev service on 127.0.0.1:18080).
  Do NOT run docker build on the shared host. Unity path: wait for valid
  UNITY credentials, then dispatch Unity Android build. Continue M1
  vertical-slice: Sudden Death (opt-in), durable persistence, queue profiles."
updated_at: "2026-09-09"
