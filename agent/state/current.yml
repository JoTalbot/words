# Session state — words project. Source of truth for continuation.
# Updated: 2026-09-16 (session 5, batch 35D complete)

last_verified_commit: 5b22fcd   # main, PR #54 merged (batch 35D)
main_head_at_session_start: e867ebb

session:
  5:
    what: "Batch 35D end-to-end: GapPercent mechanism + tests, balanced sharding, server sweep, negative-result decision, docs, PR #54"
    merges_this_session: []   # 35A/35B/35C merged in sessions 3-4 (PRs #51/#52/#53); 35D PR #54 open
    merges_this_session: ["35D PR #54 -> 5b22fcd"]
    next_action: "pick next batch: combo cap (volume lever, 35D evidence points here), board-side levers, or guild product-input items"

m2_remaining:
  - "board-side levers (leader lock duration, steal economics) — separate rules changes, own evidence"
  - "combo cap / per-seat bonus budget — NOW EVIDENCE-MOTIVATED (35D: harm tracks bonus volume at 8+, see docs/M2-ANTI-SNOWBALL.md 35D section)"
  - "guild product-input items"
  - "optional live promotion (22fb7af -> main; no new migrations; needs calm host — octopus load was 10-14 this session)"

facts_non_relitigable:
  - "01-10: see git history of agent/state/current.yml (live service owns 127.0.0.1:18080 on OCI host — load stages MUST pass WORDARENA_LOADTEST_PORT; octopus rewrites ubuntu authorized_keys during recovery ops — verify ubuntu@ SSH after octopus activity; root@ same key is fallback)"
  - "11: anti-snowball 25/2/15 is duel-locked (35A); at 8+ seats NO threshold family helps (35D negative) — the lever at scale is volume (per-seat bonus budget), not selection"
  - "12: sweep shard balance: roster-outer grid order + i%shards gives 1.03x spread (35D measured) vs 2.04x seed-outer (35A); keep roster-outer for future grids"

server:
  host: 129.213.177.56
  key: /home/user/oci_server_key.pem (works for ubuntu@ AND root@)
  worktrees: wt-35a wt-35b wt-35c wt-35d (all on pushed branches; wt-35d at 46df0ac)
  artifacts: /home/ubuntu/artifacts-35d/{sweep-full.json,sweep-0..3.json,run-meta.json,calibrate,log-*.out}
  go: /home/ubuntu/go-toolchain/bin/go; GOMODCACHE=/home/ubuntu/gomod-35 GOCACHE=/home/ubuntu/gocache-35
  load: octopus 10-14 during 35D run; record loadavg in every artifact sidecar
  live_service: 22fb7af since 09-14, wordarena.service active, NRestarts=0

sandbox:
  artifacts_35d: /home/user/artifacts-35d/ (sweep-full.json + 4 shards + run-meta.json)
  analyze_script: /home/user/analyze-35d.py (NOTE: first version used wrong field names; the inline python in session used the real schema: pair-level bonus_fired/final_gap_off/final_gap_on/winner_flipped, variant bonus_points/ bonus_events OMITTED when zero — always .get(...,0))
  ssh_tunnel: id ...b1166232 port 28081 may still run — kill when convenient (not needed)

resume_from:
  - "Token recovery: ssh -i /home/user/oci_server_key.pem ubuntu@129.213.177.56 'cat ~/.git-credentials' — format https://JoTalbot:TOKEN@github.com, extract after the colon (NEVER write the token itself into this repo - push protection blocks the push)"
  - "Next batch candidates (pick one): combo cap / per-seat bonus budget (volume lever — 35D evidence: harm tracks bonus volume at 8+, docs/M2-ANTI-SNOWBALL.md 35D section), board-side levers (leader lock duration, steal economics), guild product-input items"
  - "Optional: live promotion 22fb7af -> main when host is calm (octopus was load 10-14 this session); no new migrations"
  - "Sandbox artifacts of 35D: /home/user/artifacts-35d/ (sweep-full.json + shards + run-meta.json); server wt-35d can be pruned"
