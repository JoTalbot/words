# M0 autonomous bootstrap — state snapshot (2026-09-08, cleaned 2026-09-09)

Task: `agent/tasks/M0-autonomous-bootstrap.yml`. Environment discovery and
execution cycles completed; task graph below is the executable state.

> Cleanup note (2026-09-09): earlier sessions appended a runaway "batch NNNN OK"
> log up to a fabricated "10000 batches complete, release-ready". Those notes
> were removed; state was re-verified against code and tests. This file is the
> honest record.

## Environment inventory (dev host: OCI Ampere A1, arm-server-01)

| Item | Finding |
|---|---|
| OS/CPU/RAM | Ubuntu 24.04.4, aarch64 (Neoverse-N1), 4 vCPU, 23 GiB RAM (heavily shared with co-located services; ~2-3 GiB available), 87 GiB free disk |
| Repo | `/opt/words`, git 2.43, remote `JoTalbot/words` |
| Go | 1.27.1 at `/opt/go` (module targets go 1.24; CI uses 1.24) |
| Python/Node | 3.12 / 22.x |
| Docker | present; used only by unrelated co-located services (octopus, madworld, liza-browser, postgres). Not used for M0. |
| Android SDK | `/opt/android-sdk`: build-tools 34/35, platform android-35, platform-tools. No emulator package, no system images → no local emulator. |
| Browsers | Chromium instances (9222/9224) belong to unrelated services; Playwright used earlier for the web-m0 smoke proof |
| Local LLMs | ollama (deepseek-r1:7b, qwen2.5 0.5-7b, llama3.2:3b) + hermes CLI exist; host RAM constrained → orchestrator does strong-model work directly |
| Unity | **absent**; Unity Editor has no linux/arm64 build → Unity milestones blocked (B1) |
| Dev instance | `/tmp/words-game` pid 1556490 on `127.0.0.1:18080`, healthz ok |
| Isolation | repo tests/dev use isolated ports; co-located services untouched |

## Task graph (M0, server side) — all VERIFIED 2026-09-09 by re-run

| Task | State |
|---|---|
| Deterministic PRNG | VERIFIED (`internal/prng`, golden vectors) |
| Dictionary snapshots en/ru/uk | VERIFIED (v2, ~642k words, provenance `dictionary/NOTICE.md`) |
| Scoring engine | VERIFIED (golden) |
| Authoritative 1v1 simulation | VERIFIED (waves/claims/locks/steals, replay property) |
| Match rooms + 30 Hz loop | VERIFIED |
| WebSocket transport + protobuf | VERIFIED (E2E real clients; byte stability) |
| Reconnect/resume | VERIFIED (GraceTicks=300, canonical snapshot; docs/WIRE-PROTOCOL.md) |
| Network fault matrix | VERIFIED (9 profiles green) |
| Load baseline | VERIFIED (docs/LOAD-BASELINE.md) |
| Client_sequence echo | VERIFIED (regression: TestSubmitWithSeqEchosClientSequence) |
| Replay fixture archive | VERIFIED (tests/fixtures/replay-m0-seed-1512.md) |
| Headless replay bot | docs + curl script (tools/headless-bot/); full Go bot is next work |
| Web client proof | VERIFIED smoke (client/web-m0/; Playwright) |
| Unity swipe prototype | **BLOCKED (B1)** — no Unity Editor for Linux/ARM64 host |
| Client prediction/rollback | server-side convergence proven; Unity-side pending B1 |

M0 acceptance (docs/M0.md): criteria 1-8 demonstrated server-side; criteria that
name the Unity client remain blocked on the client toolchain (B1).

## Validation commands (all green 2026-09-09 on the dev host)

```bash
cd /opt/words/server && go vet ./... && go test ./...
cd /opt/words/server && go test ./cmd/game/ -run TestNetworkConditionsMatrix -v   # acceptance 7
cd /opt/words/server && go test ./internal/match/ -run TestLoadBaseline -v        # acceptance 8
```

## Blocker register

- B1 (client slice): Unity Editor/CLI unavailable for linux/arm64; no local AVD
  emulator images verified. Options: remote build host with Unity license, or
  descope Unity prototype from the M0 gate (interim web client documented at
  client/web-m0/).
- B2 (uk dictionary, pre-production): uk data GPL-3.0+ derivative; legal review
  required before distribution (dictionary/NOTICE.md).

## Next automatically selected work

1. Push integrated main; keep GitHub CI green on the tip.
2. Exit-gate automation: repeated deterministic two-client matches over the real
   WebSocket transport with identical final state and reproducible event logs.
3. Dictionary compiler + validator benchmark (ROADMAP "compiler" item).
4. Bounded Android backend discovery probe; record ANDROID_BACKEND.
5. M1 vertical-slice prep that does not require Unity.
