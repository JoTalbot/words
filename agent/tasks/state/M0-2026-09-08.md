# M0 autonomous bootstrap — state snapshot (2026-09-08)

Task: `agent/tasks/M0-autonomous-bootstrap.yml`. Environment discovery and
first execution cycles completed; task graph below is the executable state.

## Environment inventory (dev host: OCI Ampere A1)

| Item | Finding |
|---|---|
| OS/CPU/RAM | Ubuntu 24.04.4, aarch64 (Neoverse-N1), 4 vCPU, 23 GiB RAM (heavily shared), 93 GiB free disk |
| Repo | `/opt/words`, git 2.43, remote `JoTalbot/words`, main branch |
| Go | 1.27.1 installed at `/opt/go` (module targets go 1.24, CI uses 1.24) |
| Python/Node | 3.12 / 22.x |
| Docker | present (used by unrelated co-located services; not used for M0) |
| Android SDK | `/opt/android-sdk`: platform-tools (adb), build-tools 34/35, platform android-35 |
| Browsers/automation | Chromium instances belong to unrelated services (ports 9222/9224); not touched |
| Local LLMs | ollama models (deepseek-r1:7b, qwen2.5, llama3.2) + hermes CLI exist but host RAM is constrained; orchestrator executed M0 work directly |
| Unity | **absent**; Unity Editor has no Linux/ARM64 build → Unity client milestones are blocked on this host |
| Dev instance | game service on `127.0.0.1:18080` (`WORDARENA_ADDR`), healthz green |
| Isolation | other services on 80/443/8080/9xxx etc. untouched; repo tests/dev use isolated ports |

## Task graph (M0, server side)

| Task | State |
|---|---|
| Deterministic PRNG | VERIFIED (`internal/prng`, golden vectors) |
| Dictionary snapshots en/ru/uk | VERIFIED (v2, ~642k words, provenance in `dictionary/NOTICE.md`) |
| Scoring engine | VERIFIED (golden) |
| Authoritative 1v1 simulation | VERIFIED (waves/claims/locks/steals, replay property 80 trials) |
| Match rooms + 30 Hz loop | VERIFIED |
| WebSocket transport + protobuf | VERIFIED (E2E two real clients) |
| Reconnect/resume | VERIFIED (reconnect within match lifetime, canonical snapshot) |
| Network fault matrix (RTT/loss) | VERIFIED (9 profiles green) |
| Load baseline | VERIFIED (docs/LOAD-BASELINE.md) |
| Protocol byte stability | VERIFIED |
| Unity swipe prototype | **BLOCKED** — no Unity Editor on Linux/ARM64 host |
| Client prediction/rollback | partially proven headless (server convergence); Unity-side pending the same blocker |

M0 acceptance (docs/M0.md): items 1-8 demonstrated server-side; items that
name a Unity client remain blocked on the client toolchain.

## Validation commands

```bash
cd /opt/words/server && go vet ./... && go test ./... && go test -race ./cmd/game/ ./internal/...
cd /opt/words/server && go test ./cmd/game/ -run TestNetworkConditionsMatrix -v   # acceptance 7
cd /opt/words/server && go test ./internal/match/ -run TestLoadBaseline -v        # acceptance 8
```

## Blocker register

- B1 (client slice): Unity Editor/CLI unavailable for linux/arm64; no AVD
  emulator images verified. Options for human: provide x86_64 build host /
  Unity license server, or descope Unity prototype from M0 gate.
- B2 (uk dictionary, pre-production): uk data GPL-3.0+ derivative; legal
  review required before distribution (see dictionary/NOTICE.md).

## Next automatically selected work

1. Re-check CI on pushed commits; keep main green.
2. Session hardening: disconnect tracking, grace expiry semantics, seat
   re-entry rules.
3. Protocol: client_sequence handling policy documented + tested.
4. tools/ headless bot client for repeated remote matches (exit-gate
   evidence), and replay fixture archive.
5. M1 vertical-slice prep once the M0 client blocker is resolved.
