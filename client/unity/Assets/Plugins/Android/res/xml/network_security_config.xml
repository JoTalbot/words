<?xml version="1.0" encoding="utf-8"?>
<!-- Batch 34D: scoped cleartext exception for CI device automation.
     base-config keeps cleartext DENIED for every real origin (identical to
     the platform default on API 28+). The domain-config whitelists only the
     loopback-like hosts used by the emulator smoke: 10.0.2.2 is the AOSP
     emulator's alias for the HOST machine's 127.0.0.1 - it is not routable
     on any real device or network, so this cannot weaken a shipped build's
     transport security. Evidence: run 34906844282, logcat-fullmatch.txt
     "[WORDS_DIAG] type=Exception msg=InvalidOperationException: Insecure
     connection not allowed" while enqueueing to http://10.0.2.2:18080. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
